# RestroSync Online Ordering

Customer-facing online ordering app for RestroSync.

## Run

```bash
npm install
npm run dev
```

Set `VITE_API_URL` if the backend is not running on `http://localhost:8080`.
